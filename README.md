# Abhishek Shaw

## Stylesheets

- Use `assets/css/site.css` as the canonical stylesheet for all pages.
- `normalize.css`, `locomotive-scroll.css`, `styleguide.css`, `components.css`, and `style-new.css` are source segments that are bundled into `site.css`.